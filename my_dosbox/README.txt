To run your tests in dosbox:

1. Install dosbox
    apt-get install dosbox

2. Place a file a.asm in the directory
    This file will be deleted afterwards so make sure you have it

3. Run ./run.sh 
    Not from an ssh session
